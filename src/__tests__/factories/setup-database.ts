export default async function setupDatabase() {
  // try {
  //   const res = await series([() => exec('npm run migration:run:test')]);
  //   console.log(res);
  // } catch (e) {
  //   console.log(e);
  // }
}
